function conditon(car) {
  let pridiction = null;
  if (car === "KIA") {
    pridiction = true;
    console.log(pridiction);
  } else if (car !== "KIa") {
    pridiction = true;

    console.log(pridiction);
  } else if (car === "toyota") {
    pridiction = true;

    console.log(pridiction);
  }
}
conditon("KIA");
